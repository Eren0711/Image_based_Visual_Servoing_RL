numpy>=1.24
scipy>=1.10
gymnasium>=0.29
stable-baselines3>=2.3
matplotlib>=3.7
pyyaml>=6.0
tensorboard>=2.14
tqdm>=4.65
rich>=13.0

# Historical safety and reporting tools. New installations may instead use
# the explicit extras in pyproject.toml: .[safety], .[reports], or .[all].
quadprog>=0.1.11
tbparse>=0.0.9
Pillow>=10.0

# Repository acceptance tests.
pytest>=8.0
pytest-cov>=5.0
